streamlit>=1.30.0
openai>=1.6.0
pypdf2>=3.0.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
numpy>=1.21.0
# Check the status to see your changed file
git status

# Add the file to your commit
git add requirements.txt

# Commit the change with a message
git commit -m "Fix faiss dependency to faiss-cpu"

# Push the change to GitHub
git push
